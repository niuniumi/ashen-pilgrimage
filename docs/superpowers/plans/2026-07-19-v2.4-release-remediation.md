# 灰烬圣途 v2.4 Release Remediation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 收口既有 v2.4 视听升级，修复所有已复现的交互、排版、结算与存档缺陷，并通过 GitHub Pages 发布可验证的 v2.4 正式版。

**Architecture:** 在现有 Phaser/Vite 架构上增加少量可测试的纯策略模块（阶段一致性、中文排版、动效、地图键盘、无障碍桥），让场景只消费明确结果。保留既有 v2.4 lossless WebP、音频混音与角色选择成果；发布仍由受保护的 CI → Pages 工作流完成。

**Tech Stack:** Phaser 3.90、Vite 7、Node 24 `node:test`、Playwright 1.61、GitHub Actions/Pages、PowerShell。

## Global Constraints

- 唯一实施目录是 `C:\Users\16224\Desktop\灰烬圣途-重做\.worktrees\v2.4-polish`，分支 `codex/v2.4-polish`；不得覆盖用户已有未提交 v2.4 改动。
- 保留“黑铁圣书 / 深暗像素手稿”视觉；不得引入圆角玻璃卡、霓虹渐变或通用 SaaS 风格。
- 只有 selectable 地图节点可交互；地图 pointer 中心和四边回归都必须通过。
- 结算状态、非战斗节点阶段、checkpoint、pending reward、result 必须保持事务一致。
- 长中文必须真实换行且序章四页位于面板正文区内。
- 低动态必须落最终状态且不留下循环 tween；音频必须支持 pointer/keyboard 解锁与页面生命周期。
- 正式发布只到 `https://niuniumi.github.io/ashen-pilgrimage/`，必须等待远端 CI、Pages 与线上 smoke 成功。
- 清理不得删除产品源码、测试、生产资源、授权或可复现生成源；不不可恢复删除归属不明的用户文件。

---

### Task 1: 固化既有 v2.4 视听升级基线

**Files:**
- Preserve: 当前 `git status --short` 中全部既有 v2.4 源码、测试、授权、生成脚本与生产资源
- Add: `docs/superpowers/specs/2026-07-19-v2.4-release-remediation-design.md`
- Add: `docs/superpowers/plans/2026-07-19-v2.4-release-remediation.md`

**Interfaces:**
- Produces: 一个可回溯的 v2.4 基线提交；后续任务只在该提交之上追加。

- [ ] **Step 1: 记录现状并确认没有测试污染**

Run:

```powershell
$env:PATH="C:\Users\16224\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin;$env:PATH"
git status --short
pnpm test
pnpm assets:verify
pnpm qa:design-tokens
pnpm qa:asset-manifest
pnpm qa:visual-bindings
pnpm build
```

Expected: `177` tests pass, `40/40` runtime images pass, all QA/build exit `0`.

- [ ] **Step 2: 审阅新增第三方图标授权与运行时引用**

Run:

```powershell
rg -n "license|author|source|game-icons" THIRD_PARTY_ASSETS.md qa/source-art/icon-vectors scripts/import-game-icon-vectors.mjs
pnpm qa:content-schema
```

Expected: 所有导入图标有作者/许可证记录；内容 schema exit `0`。

- [ ] **Step 3: 提交已有成果与本设计/计划**

```powershell
git add -A
git commit -m "feat: stage v2.4 audiovisual polish"
```

Expected: working tree clean；commit 包含 v2.4 图标/卡面/角色选择/音频混音与对应测试。

---

### Task 2: 修复结算事务、暂停退出与存档迁移

**Files:**
- Create: `src/game/RunStagePolicy.js`
- Modify: `src/game/RunMigration.js`
- Modify: `src/game/RunResume.js`
- Modify: `src/game/SaveManager.js`
- Modify: `src/ui/PauseMenu.js`
- Modify: `src/scenes/BattleScene.js`
- Test: `tests/run-stage-policy.test.mjs`
- Test: `tests/run-migration.test.mjs`
- Test: `tests/run-resume.test.mjs`
- Test: `tests/save-manager.test.mjs`
- Test: `qa/pause-menu-regression.mjs`

**Interfaces:**
- Produces: `isSettlementStage(stage)`, `isNodeStage(stage)`, `isNodeStageCompatible(run)`, `normalizeRunStage(run)`, `prepareMainMenuExit(run)`, `prepareMapExit(run)`.

- [ ] **Step 1: 写失败测试覆盖三种结算、旧非战斗节点与缺失 relics**

```js
assert.equal(prepareMapExit({ pendingScene: 'reward' }).ok, false);
assert.equal(prepareMapExit({ pendingScene: 'act-clear' }).ok, false);
assert.equal(prepareMapExit({ pendingScene: 'result' }).ok, false);
assert.deepEqual(prepareMainMenuExit(runWithCheckpoint).run, runWithCheckpoint);
assert.equal(migrateRun(legacyMidChest).pendingScene, null);
assert.deepEqual(migrateRun({ ...validRun, relics: undefined }).relics, []);
assert.equal(SaveManager.saveRun({ ...validRun, relics: ['unknown-relic'] }), false);
```

Run: `node --test tests/run-stage-policy.test.mjs tests/run-migration.test.mjs tests/run-resume.test.mjs tests/save-manager.test.mjs`

Expected: FAIL on missing policy, orphan stage and relic validation.

- [ ] **Step 2: 实现纯阶段策略**

```js
const SETTLEMENT_STAGES = new Set(['reward', 'act-clear', 'result']);
const NODE_STAGE_TYPES = {
  battle: new Set(['battle', 'elite', 'boss']),
  'boss-intro': new Set(['boss']),
  reward: new Set(['battle', 'elite']),
  event: new Set(['event']), shop: new Set(['shop']),
  rest: new Set(['rest']), chest: new Set(['chest'])
};

export function isSettlementStage(stage) { return SETTLEMENT_STAGES.has(stage); }
export function isNodeStage(stage) { return Object.hasOwn(NODE_STAGE_TYPES, stage); }
export function isNodeStageCompatible(run) {
  const node = run?.map?.nodes?.find((item) => item.id === run?.map?.activeNode);
  return Boolean(node && NODE_STAGE_TYPES[run.pendingScene]?.has(node.type));
}
export function prepareMainMenuExit(run) { return { ok: Boolean(run), run }; }
export function prepareMapExit(run) {
  if (!run || isSettlementStage(run.pendingScene)) return { ok: false, run };
  return { ok: true, run };
}
```

`normalizeRunStage` 必须在 node stage 不相容时清空 `pendingScene/pendingBattleType/checkpoint/pendingReward`，并调用现有 `rollbackOrphanedNode`。

- [ ] **Step 3: 接入迁移、保存和暂停菜单**

```js
run.relics = Array.isArray(run.relics) ? run.relics : [];
normalizeRunStage(run);
```

`SaveManager.isValidRun` 增加 `Array.isArray(run.relics)` 与 `run.relics.every(id => Boolean(getRelic(id)))`；`goMainMenu()` 只保存原状态，`goMap()` 在 settlement stage 返回并显示错误反馈，普通战斗主动放弃才调用 `MapSystem.abandonActiveNode`。

- [ ] **Step 4: Playwright 验证 850ms 窗口**

Run: `node qa/pause-menu-regression.mjs --url=http://127.0.0.1:4193/`

Expected: 普通胜利继续 Reward、Boss 胜利继续 ActClear、失败继续 Result；主菜单往返保留 checkpoint；返回地图按钮在三种结算态不可破坏状态。

- [ ] **Step 5: 提交**

```powershell
git add src/game src/ui/PauseMenu.js src/scenes/BattleScene.js tests qa/pause-menu-regression.mjs
git commit -m "fix: preserve run settlement transactions"
```

---

### Task 3: 统一中文排版与低动态叙事

**Files:**
- Create: `src/ui/CjkTextLayout.js`
- Create: `src/game/MotionPolicy.js`
- Modify: `src/scenes/PrologueScene.js`
- Modify: `src/ui/StoryDialog.js`
- Modify: `src/ui/UIOrnament.js`
- Modify: `src/game/SaveManager.js`
- Test: `tests/cjk-text-layout.test.mjs`
- Test: `tests/motion-policy.test.mjs`
- Create: `scripts/qa-prologue-layout.mjs`

**Interfaces:**
- Produces: `wrapMeasuredText(text, maxWidth, measure)`, `cjkWordWrap(width)`, `motionDuration(settings, fullMs)`, `isMotionEnabled(settings)`.

- [ ] **Step 1: 写失败的纯排版/动效测试**

```js
const wrapped = wrapMeasuredText('今夜墓园深处传来第十三声钟鸣。', 8, (s) => [...s].length);
assert.ok(wrapped.split('\n').every((line) => [...line].length <= 8));
assert.equal(/^[，。！？；：、）】》”’]/m.test(wrapped), false);
assert.equal(/[（【《“‘]$/m.test(wrapped), false);
assert.equal(motionDuration({ animation: false }, 460), 0);
```

Run: `node --test tests/cjk-text-layout.test.mjs tests/motion-policy.test.mjs`

Expected: FAIL because modules do not exist.

- [ ] **Step 2: 实现真实测宽 CJK callback**

```js
export function cjkWordWrap(width) {
  return {
    width,
    callback(text, textObject) {
      return wrapMeasuredText(text, width, (value) => textObject.context.measureText(value).width);
    }
  };
}
```

`wrapMeasuredText` 用 `Intl.Segmenter('zh-CN', { granularity: 'grapheme' })`，无该 API 时回退 `Array.from`；保留原始空行；处理常见中文开/闭标点禁则。

- [ ] **Step 3: 接入序章、StoryDialog 和 reduced motion**

序章正文使用 `wordWrap: cjkWordWrap(730)`；翻页/淡入时长统一经过 `motionDuration`；reduced 时直接显示最终页。StoryDialog reduced 时一次显示整句，正文 hit layer 缩到不覆盖继续/跳过按钮。UIOrnament hover 在 reduced 时直接设置最终状态，不建立 tween。

`SaveManager.readSettings()` 在已存设置没有自有 `animation` 字段时使用 `defaultsForDevice().animation`，而不是 `defaultSettings.animation`。

- [ ] **Step 4: 运行四页真实渲染边界测试**

Run: `node scripts/qa-prologue-layout.mjs --url=http://127.0.0.1:4193/`

Expected: 4/4 页 `body.width <= 730`，bounds 位于面板正文区内，正文 bottom 小于页码 top；1280×720、1366×768、1536×864 均通过。

- [ ] **Step 5: 提交**

```powershell
git add src/ui/CjkTextLayout.js src/game/MotionPolicy.js src/scenes/PrologueScene.js src/ui/StoryDialog.js src/ui/UIOrnament.js src/game/SaveManager.js tests scripts/qa-prologue-layout.mjs
git commit -m "fix: contain Chinese narrative text"
```

---

### Task 4: 修复地图输入并补齐键盘、读屏和手机方向策略

**Files:**
- Create: `src/input/MapInputController.js`
- Create: `src/game/AccessibilityBridge.js`
- Modify: `src/scenes/MapScene.js`
- Modify: `src/scenes/PrologueScene.js`
- Modify: `src/scenes/MainMenuScene.js`
- Modify: `src/scenes/SceneHelpers.js`
- Modify: `src/main.js`
- Modify: `index.html`
- Test: `tests/map-input.test.mjs`
- Test: `tests/accessibility-bridge.test.mjs`
- Modify: `scripts/qa-progression-regression.mjs`
- Create: `scripts/qa-accessibility-responsive.mjs`

**Interfaces:**
- Produces: `MapInputController`, `AccessibilityBridge.announce()`, `AccessibilityBridge.setActions()`.

- [ ] **Step 1: 写失败测试覆盖重叠边缘与键盘选择**

```js
const controller = new MapInputController([{ id: 'n1', x: 640, y: 652 }, { id: 'n2', x: 914, y: 652 }]);
assert.equal(controller.move('right'), 'n2');
assert.equal(controller.confirm(), 'n2');
```

Playwright 必须在 n2 中心和 `(x, y-22)`、`(x, y+22)`、`(x-22, y)`、`(x+22, y)` 分别重置存档后点击，并断言都进入 n2。

- [ ] **Step 2: 只给 selectable 节点输入**

```js
if (selectable) {
  const hit = this.add.zone(pos.x, pos.y, 100, 100).setDepth(60);
  hit.setInteractive({ useHandCursor: true });
  hit.on('pointerup', () => this.selectNode(node));
}
```

锁定/完成节点不创建 pointer zone、不显示手型；可选节点保留 v2.4 aura，pointerdown 压下、pointerout 恢复；`selectNode` 增加一次性 transition lock。

- [ ] **Step 3: 接入地图/序章/主菜单键盘与语义桥**

`MapInputController` 以几何方向选择最近可选节点；方向键/WASD 移动，Enter/Space 确认。序章左右键翻页、Enter 继续、Escape 跳过。主菜单上下键移动、Enter 激活。`AccessibilityBridge` 创建常驻隐藏 live region 和当前场景 action buttons，并在场景 shutdown 时清空旧 action。

- [ ] **Step 4: 增加手机竖屏遮罩**

```css
@media (orientation: portrait) and (max-width: 700px) {
  #rotate-device { display: grid; pointer-events: auto; }
  canvas { pointer-events: none; }
}
```

遮罩文案为“请旋转设备 / 横屏展开圣途”，使用现有煤黑、烛金、像素边框；横屏恢复 canvas pointer。

- [ ] **Step 5: 运行 pointer、键盘、读屏 DOM 与横竖屏 QA**

Run:

```powershell
node scripts/qa-progression-regression.mjs --url=http://127.0.0.1:4193/
node scripts/qa-accessibility-responsive.mjs --url=http://127.0.0.1:4193/
```

Expected: 5 个 n2 点击点全部进入；主菜单→角色→地图→节点可全键盘推进；live region/按钮存在；390×844 显示旋转遮罩且 canvas 不接收 pointer；844×390 遮罩消失。

- [ ] **Step 6: 提交**

```powershell
git add src/input/MapInputController.js src/game/AccessibilityBridge.js src/scenes src/main.js index.html tests scripts/qa-progression-regression.mjs scripts/qa-accessibility-responsive.mjs
git commit -m "fix: make route input reliable and accessible"
```

---

### Task 5: 音频生命周期、动效收口与视效验收

**Files:**
- Modify: `src/game/AudioManager.js`
- Modify: `src/scenes/SceneHelpers.js`
- Modify: `tests/audio-manager-polish.test.mjs`
- Modify: `qa/audio-runtime.mjs`
- Modify: `scripts/qa-responsive-facing.mjs`
- Modify: `docs/RELEASE_NOTES_2.4.md`

**Interfaces:**
- Produces: `AudioManager.unlock()` 可恢复 AudioContext；`AudioManager.handleVisibilityChange(hidden)`。

- [ ] **Step 1: 写失败测试覆盖 keyboard unlock 和生命周期**

```js
await manager.unlock();
assert.equal(context.resumeCalls, 1);
manager.handleVisibilityChange(true);
assert.equal(bgm.pauseCalls, 1);
manager.handleVisibilityChange(false);
assert.equal(bgm.resumeCalls, 1);
```

Run: `node --test tests/audio-manager-polish.test.mjs`

Expected: FAIL on missing context resume/lifecycle methods.

- [ ] **Step 2: 实现可见性与键盘解锁**

`unlock()` 在 context state 为 `suspended` 时 await `context.resume()`；成功后才标记 unlocked。SceneHelpers 同时安装一次 pointerdown/keydown 解锁。`visibilitychange/pagehide` 暂停 BGM；重新 visible 时仅在 unlocked 且设置允许音乐时 resume 并淡入目标音量。

- [ ] **Step 3: 视听/动效验收**

Run:

```powershell
node qa/audio-runtime.mjs --url=http://127.0.0.1:4193/
pnpm qa:responsive-facing
pnpm qa:pixel-scenes
pnpm qa:role-matrix
```

Expected: 0 console/page errors；BGM/SFX 音量、duck、cooldown、hide/resume、键盘解锁均通过；v2.4 高精度图标/卡面/角色立绘在 1280、1366、1536、1920 截图保持边界与层级。

- [ ] **Step 4: 提交**

```powershell
git add src/game/AudioManager.js src/scenes/SceneHelpers.js tests/audio-manager-polish.test.mjs qa/audio-runtime.mjs scripts/qa-responsive-facing.mjs docs/RELEASE_NOTES_2.4.md
git commit -m "feat: finalize v2.4 audiovisual feedback"
```

---

### Task 6: 全矩阵验证、合并、Pages 发布与安全清理

**Files:**
- Modify: `src/game/Version.js`
- Modify: `package.json`
- Modify: `docs/PRODUCTION_VERIFICATION.md`
- Modify: `README.md`
- Preserve: `.github/workflows/ci.yml`, `.github/workflows/pages.yml`

**Interfaces:**
- Produces: `v2.4.0` 生产提交、成功的 CI/Pages 运行、线上 smoke 证据与干净源码树。

- [ ] **Step 1: 版本与完整本地门禁**

Run:

```powershell
pnpm test
pnpm assets:verify
pnpm qa:design-tokens
pnpm qa:content-schema
pnpm qa:asset-manifest
pnpm qa:visual-bindings
pnpm qa:battle-mechanics
pnpm qa:battle-layout
pnpm qa:simulation
pnpm qa:resume-stages
pnpm qa:map-migration
pnpm qa:progression
pnpm qa:release-flow
pnpm qa:resource-budget
pnpm build
```

Expected: 全部 exit `0`；测试数不少于 177；runtime assets 不少于 40；simulation 无 timeout；Vite build success。

- [ ] **Step 2: 最终代码审查与提交**

```powershell
git diff --check main...HEAD
git status --short
git commit -am "release: prepare Ashen Pilgrimage v2.4.0"
```

Expected: reviewer 无 Critical/Important；分支 clean。

- [ ] **Step 3: 合并 main 并推送**

```powershell
git -C C:\Users\16224\Desktop\灰烬圣途-重做 merge --ff-only codex/v2.4-polish
git -C C:\Users\16224\Desktop\灰烬圣途-重做 push origin main
```

Expected: `origin/main` SHA 与本地 main 相同。

- [ ] **Step 4: 等待并验证 CI、Pages 和线上产品**

检查 GitHub Actions 最新 CI 与 Pages workflow 都为 success；再运行：

```powershell
pnpm qa:deploy-smoke -- --url=https://niuniumi.github.io/ashen-pilgrimage/
```

Expected: 线上 `BUILD_VERSION=v2.4.0`，序章、地图边缘点击、角色/战斗/暂停均通过，0 page errors。

- [ ] **Step 5: 精确清理生成物与旧服务**

先枚举并确认绝对路径，再清理 `dist`、忽略的 QA screenshots、`.codex_audio_tmp`、`.netlify` 本地状态、Vite/服务器日志、临时 clone 与已合并 worktrees；停止真实 Node 预览 PID，不读取陈旧 pid 文件杀进程。旧 `New project1` 只清理 `.superpowers`、`docs/superpowers`、服务日志与空目录，保留其运行源文件。

- [ ] **Step 6: 最后验证与资源管理器交付**

```powershell
git -C C:\Users\16224\Desktop\灰烬圣途-重做 status --short
git -C C:\Users\16224\Desktop\灰烬圣途-重做 rev-parse HEAD
git -C C:\Users\16224\Desktop\灰烬圣途-重做 rev-parse origin/main
Start-Process explorer.exe -ArgumentList '/select,', 'C:\Users\16224\Desktop\灰烬圣途-重做\README.md'
```

Expected: status clean；三个 SHA（release/本地/远端）一致；线上 smoke success；Explorer 打开正式产品目录。
